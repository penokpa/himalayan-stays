import ForgotPasswordForm from "./forgot-form";

export const metadata = {
  title: "Forgot Password | Himalayan Stays",
  robots: { index: false, follow: false },
};

export default function ForgotPasswordPage() {
  return <ForgotPasswordForm />;
}
